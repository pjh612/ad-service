spring:
  application:
    name: ad-service
  datasource:
    url: jdbc:mysql://localhost:3306/ad
    password: password
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
server:
  port: 8080
client:
  business:
    url: https://api.odcloud.kr
    serviceKey: ${NTS_SERVICE_KEY}
secret: adserviceserviceadadserviceserviceadadserviceserviceadadserviceserviceadadserviceservicead